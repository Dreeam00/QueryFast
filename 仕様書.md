📘 QueryFast — API 仕様書（引数・返り値・動作）
以下は 実装者向けの正式仕様。
コードは書かない。
引数・返り値・動作だけを完全に定義する。

1. Q(selector)
引数
名前	型	説明
selector	string / Node / Node[] / QCollection	DOM を取得するための入力


返り値
QCollection

動作
string → querySelectorAll

Node → 1 要素の QCollection

Node[] → そのまま QCollection

QCollection → そのまま返す

それ以外 → 空の QCollection

2. Q.add(selector)
引数
名前	型	説明
selector	string	'div#id.class1.class2' の簡易 CSS-like 文字列


返り値
QCollection（新規 DOM 要素 1 個）

動作
tag / id / class をパースして element を生成

QCollection に包んで返す

3. QCollection.prototype.appendTo(target)
引数
名前	型	説明
target	string / Node	親要素


返り値
this（QCollection）

動作
target が string → querySelector

target が Node → そのまま

全要素を appendChild

4. QCollection.prototype.content(value)
引数
名前	型	説明
value	string（省略可）	テキスト内容


返り値
value 省略 → string（最初の要素の textContent）

指定あり → this

動作
textContent を設定 or 取得

5. QCollection.prototype.html(value)
引数
名前	型
value	string（省略可）


返り値
value 省略 → string（innerHTML）

指定あり → this

6. QCollection.prototype.attr(name, value)
引数
名前	型	説明
name	string	属性名
value	string / null / false / undefined	値


返り値
value 省略 → string or null

指定あり → this

動作
value が null/false/undefined → removeAttribute

それ以外 → setAttribute

7. QCollection.prototype.on(type, handler)
引数
名前	型	説明
type	string	"click", "input", "hover" など
handler	function	イベントハンドラ


handler の引数
名前	型	説明
el	Element	対象 DOM
eventOrActive	Event / boolean	通常イベント → Event、hover → boolean
event	Event（hover のときのみ）	mouseenter/mouseleave の Event


返り値
this

特殊仕様（hover）
"hover" の場合：

handler(el, true, event) → mouseenter

handler(el, false, event) → mouseleave

8. QCollection.prototype.ui(styleObj)
引数
名前	型	説明
styleObj	object	{ key: value }


特別キー
key	意味
bg	background
text	color


返り値
this

9. Q.state(initial)
引数
名前	型
initial	any


返り値
Proxy オブジェクト

プロパティ：value

メソッド：_subscribe(fn)

動作
state.value = x → 購読者に通知

_subscribe(fn) → 購読登録＋即時実行

10. QCollection.prototype.bind(state, mapFn, mode)
引数
名前	型	説明
state	Proxy（Q.state の返り値）	リアクティブ値
mapFn	function	(value) => 出力
mode	string	"text", "html", "attr", "class", "css"


返り値
this

mode ごとの mapFn 出力仕様
text
出力：string

html
出力：string（HTML）

attr
出力：{ attrName: value }

class
出力：{ className: boolean }

css
出力：{ key: value }

bg / text は特別扱い

11. Q.component(name, factory)
引数
名前	型	説明
name	string	コンポーネント名
factory	function	UI を返す関数


factory の返り値
QCollection（必須）

返り値
なし

12. Q.use(name)
引数
名前	型
name	string


返り値
QCollection（factory の返り値）

13. Q.mount(name, target)
引数
名前	型
name	string
target	string / Node


返り値
QCollection（mount された要素）